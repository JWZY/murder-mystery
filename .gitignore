node_modules
dist
.vite
*.local
*.tsbuildinfo
.DS_Store

# Secrets — never commit
.env
.env.*
!.env.example

# Private reference data — real people's past responses. NEVER commit/publish.
Murder Mystery Planning/

# AI-bridge working dir — `npm run export:world` dumps real intake answers here.
planning/

# Local design/effects reference dumps — not part of the build.
ink-effects/
smoke-and-blood/

# Relationship-web brainstorm — a local-only duplicate of the planning canvas.
# Dev-only (served by `vite` at /brainstorm.html); never built or deployed.
brainstorm.html
src/brainstorm/

# QuickEntry (Xcode)
extras/QuickEntry/build/
extras/QuickEntry/DerivedData/
*.xcuserstate
xcuserdata/
